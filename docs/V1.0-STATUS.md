# V1.0 status

Code integration is complete on `dev` and GitHub CI run 125 passed Riot data validation, TFT-only validation, and the Next.js production build.

Release is intentionally held before merging to `main` so the Supabase workspace project/schema and Vercel public Supabase environment variables can be configured first. This preserves the one-Production-build release plan for the Vercel Hobby project.
